import { Text, View } from "react-native";
const Banner = ({  }) => {
    return (
        <View style={{
            marginTop: 20
        }}>
            
        </View>
    );
};
export default Banner;